name: IPTV 筛选与格式转换

on:
  schedule:
    - cron: '25,55 * * * *' 
  workflow_dispatch:

jobs:
  filter-and-convert:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    concurrency:
      group: iptv-sync
      cancel-in-progress: true

    steps:
      - name: 📥 Checkout 仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 设置 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 安装依赖
        run: pip install requests

      - name: 1. 运行速度筛选 (实时日志模式)
        env:
          PYTHONUNBUFFERED: "1"  # 强制实时输出日志
        run: python py/speed_filter.py

      - name: 2. 运行 M3U 转换及其他脚本
        run: |
          python py/convert_to_m3u.py || echo "跳过"
          python py/zubo.pgen_custom_list.py || echo "跳过"
          python py/convert_full_m3u.py || echo "跳过"
          python generate_index.py || echo "跳过"

      - name: 📤 提交并推送更新
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"

          # 确保追踪所有相关文件
          touch py/blacklist.txt
          git add py/*.txt py/*.m3u test/*.m3u index.html || true

          if git diff --staged --quiet; then
            echo "✅ 内容未发生实质变化，跳过推送。"
          else
            echo "🚀 发现变更，正在同步至仓库..."
            git commit -m "自动更新：线路维护与黑名单 ($(date +'%Y-%m-%d %H:%M'))"
            
            # 解决冲突的关键：先拉取并以本地新生成的为准
            git pull --rebase -X ours origin main
            git push origin main
          fi
